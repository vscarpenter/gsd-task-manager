import { describe, it } from 'vitest';

describe('encryption-helpers (removed)', () => {
  // Encryption helpers were removed during PocketBase migration.
  // PocketBase stores tasks as plaintext (user owns the server).
  it.skip('module removed — no tests needed', () => {});
});
