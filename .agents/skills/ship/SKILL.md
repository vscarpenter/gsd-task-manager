# Ship Changes
1. Run typecheck, lint, and tests; fix any warnings proactively
2. Stage and commit using project's commit-message style
3. Push to origin and open a PR
4. Verify CI is green AND deployed artifacts actually contain the change
5. Report PR URL and deployment URL
